services:
  - type: web
    name: nevo-backend
    runtime: python
    plan: starter
    buildCommand: python -m pip install --upgrade pip && python -m pip install .
    startCommand: alembic upgrade head && uvicorn nevo.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
